To compile the code simply use g++ badSort.cpp or badSortTime.cpp to compile each program respectively
To run badSort, use the executable followed by the decimal value of a ie a.out .75
To run badSortTime, simply run the executable